#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    if(num %5==0){
        printf("divisable by 5:");}
        else {
        printf("not divisable by 5:");}
        
    return 0;
}